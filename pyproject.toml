[project]
name = "nuc"
version = "0.1.0"
description = "Infrared detector radiometric calibration (NUC) simulation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
]
